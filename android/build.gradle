buildscript {
    repositories {
        google()
        jcenter()
    }

    /*dependencies {
        classpath 'com.android.tools.build:gradle:7.4.2' // Ensure this line is included and up-to-date
        classpath 'com.google.gms:google-services:4.4.2' // Keep this here to include the Google services classpath
    }*/
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
